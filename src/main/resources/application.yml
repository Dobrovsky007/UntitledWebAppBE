spring:
  profiles:
    active: dev

# Default logging configuration
logging:
  level:
    com.webapp.Eventified: DEBUG
    org.springframework.security: DEBUG

# Default security configuration
app:
  security:
    enabled: true  # This will be overridden by profiles

# API base path
server:
  servlet:
    context-path: /api