spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASS}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: "noreply.eventifyteam@gmail.com"
    password: "xvqwnybieoqyzcto"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enabled: true

server:
  port: 8080
  servlet:
    context-path: /api

# Production mode - Security enabled with JWT
app:
  security:
    enabled: true

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:3600000}

# Swagger/OpenAPI configuration for production (available for frontend team)
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

# CORS configuration for production
cors:
  allowed-origins: 
    - "${FRONTEND_URL:http://localhost:3000}"
    - "${FRONTEND_URL_SECONDARY:}"
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# Spring Boot Actuator endpoints for health checks
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
