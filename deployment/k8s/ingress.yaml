# Backend ingress is handled by the frontend ingress configuration
# This file can be removed or kept for backend-specific routes only
# The main routing is defined in UntitledWebAppFE/deployment/k8s/ingress.yaml
  # Uncomment below for HTTPS
  # tls:
  #   - hosts:
  #       - eventified-api.local
  #     secretName: eventified-api-tls