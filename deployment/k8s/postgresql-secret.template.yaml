apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: eventified
type: Opaque
data:
  # Base64-encoded value of your PostgreSQL password.
  # Example: echo -n "yourStrongPassword" | base64
  POSTGRES_PASSWORD: REPLACE_WITH_BASE64
